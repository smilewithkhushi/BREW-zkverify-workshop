# Proof artifacts (binary, regenerated by bb)
magic_square/target/proof
magic_square/target/vk
magic_square/target/public_inputs
magic_square/target/magic_square.gz

# Keep the compiled circuit artifact
!magic_square/target/magic_square.json

# ZKVerify output
proof-submission/aggregation.json

# Environment variables
proof-submission/.env
proof-submission/.env.*
!proof-submission/.env.example

# Node dependencies
proof-submission/node_modules/
proof-submission/package-lock.json

# macOS
.DS_Store
